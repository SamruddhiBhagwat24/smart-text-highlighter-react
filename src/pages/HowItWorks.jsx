function HowItWorks() {
  return (
    <div>
      <h2>How It Works</h2>
<p>
Enter any paragraph or article into the text box.
When you click <b>Generate Highlights</b>, the text is sent to an AI model
that understands context and extracts only the most important ideas.
</p>
<p>
The result is a short, clear list of key points — saving time and effort.
</p>
    </div>
  );
}

export default HowItWorks;
